import Testing
@testable import ADIFKit

/// Placeholder so the skeleton has a runnable test target. Replaced by the real
/// round-trip suite (DESIGN.md §11) once the parser exists.
@Test func moduleBuilds() {
    #expect(ADIFKit.targetSpecVersion == "3.1.6")
}
